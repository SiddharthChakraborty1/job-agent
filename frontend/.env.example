# Google OAuth client ID (same value as GOOGLE_CLIENT_ID in the repo-root .env)
VITE_GOOGLE_CLIENT_ID=

# Production backend URL (leave empty for local dev — Vite proxies /api to localhost:8000)
# Example: https://resume-job-finder-api.onrender.com
VITE_API_URL=
